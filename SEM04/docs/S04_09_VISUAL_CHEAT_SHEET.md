# Fișă vizuală: procesare de text
## Referință rapidă — Regex, GREP, SED, AWK, NANO

> **Sisteme de Operare** | Academia de Studii Economice din București - CSIE
> **Seminarul 4** | Card de referință rapidă
> **Format:** A4, prietenos pentru tipărire

---

```
╔══════════════════════════════════════════════════════════════════════════╗
║                                                                          ║
║   ████████╗███████╗██╗  ██╗████████╗    ██████╗ ██████╗  ██████╗        ║
║   ╚══██╔══╝██╔════╝╚██╗██╔╝╚══██╔══╝    ██╔══██╗██╔══██╗██╔═══██╗       ║
║      ██║   █████╗   ╚███╔╝    ██║       ██████╔╝██████╔╝██║   ██║       ║
║      ██║   ██╔══╝   ██╔██╗    ██║       ██╔═══╝ ██╔══██╗██║   ██║       ║
║      ██║   ███████╗██╔╝ ██╗   ██║       ██║     ██║  ██║╚██████╔╝       ║
║      ╚═╝   ╚══════╝╚═╝  ╚═╝   ╚═╝       ╚═╝     ╚═╝  ╚═╝ ╚═════╝        ║
║                                                                          ║
║                      FIȘĂ DE SINTEZĂ - SEMINARUL 4                      ║
║                                                                          ║
╚══════════════════════════════════════════════════════════════════════════╝
```

---

# REGEX — EXPRESII REGULATE

## Metacaractere de bază

```
┌────────────┬────────────────────────────────────┬────────────────────────┐
│ Symbol     │ Meaning                            │ Example                │
├────────────┼────────────────────────────────────┼────────────────────────┤
│     .      │ Any character (exactly 1)          │ a.c → abc, a1c, aXc   │
│     ^      │ Beginning of line                  │ ^Hello → "Hello..."   │
│     $      │ End of line                        │ end$ → "...end"       │
│     *      │ 0 or more (preceding)              │ ab*c → ac, abc, abbc  │
│     \      │ Escape special character           │ \. → literal dot      │
│    [ ]     │ Character set                      │ [aeiou] → vowels      │
│   [^ ]     │ Set negation                       │ [^0-9] → non-digits   │
└────────────┴────────────────────────────────────┴────────────────────────┘
```

## BRE vs ERE — diferențe critice

```
┌──────────────────┬───────────────────┬───────────────────┐
│ Function         │ BRE (grep)        │ ERE (grep -E)     │
├──────────────────┼───────────────────┼───────────────────┤
│ 1 or more        │      \+           │       +           │
│ 0 or 1           │      \?           │       ?           │
│ Exactly n        │    \{n\}          │      {n}          │
│ Range n-m        │   \{n,m\}         │     {n,m}         │
│ Grouping         │    \( \)          │      ( )          │
│ Alternative      │      \|           │       |           │
└──────────────────┴───────────────────┴───────────────────┘

💡 RECOMANDARE: Folosiți întotdeauna grep -E pentru a evita escaparea!

*(`grep` este, probabil, comanda pe care o folosesc cel mai frecvent. Simplă, rapidă, eficientă.)*

```

## Cuantificatori

```
┌────────────┬────────────────────────────────────┬────────────────────────┐
│ Pattern    │ Meaning                            │ Example                │
├────────────┼────────────────────────────────────┼────────────────────────┤
│    *       │ 0 or more                          │ a* → "", a, aa, aaa   │
│    +       │ 1 or more (ERE)                    │ a+ → a, aa, aaa       │
│    ?       │ 0 or 1 (ERE)                       │ colou?r → color/colour│
│   {n}      │ Exactly n                          │ a{3} → aaa            │
│  {n,}      │ Minimum n                          │ a{2,} → aa, aaa, ...  │
│  {n,m}     │ Between n and m                    │ a{2,4} → aa, aaa, aaaa│
└────────────┴────────────────────────────────────┴────────────────────────┘
```

## Clase de caractere POSIX

```
┌────────────────┬────────────────────────────────┬──────────────────────┐
│ Class          │ Equivalent                     │ Description          │
├────────────────┼────────────────────────────────┼──────────────────────┤
│ [[:alpha:]]    │ [a-zA-Z]                       │ Letters              │
│ [[:digit:]]    │ [0-9]                          │ Digits               │
│ [[:alnum:]]    │ [a-zA-Z0-9]                    │ Alphanumeric         │
│ [[:space:]]    │ [ \t\n\r\f\v]                  │ Whitespace           │
│ [[:upper:]]    │ [A-Z]                          │ Uppercase            │
│ [[:lower:]]    │ [a-z]                          │ Lowercase            │
│ [[:punct:]]    │ Punctuation                    │ .,;:!? etc.          │
└────────────────┴────────────────────────────────┴──────────────────────┘
```

## Pattern‑uri uzuale

```bash

*Notă personală: prefer scripturile Bash pentru automatizări simple și Python atunci când logica devine complexă. Este o chestiune de pragmatism.*

# IP Address (basic)
grep -oE '([0-9]{1,3}\.){3}[0-9]{1,3}' file.txt

# Email (simplified)
grep -oE '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' file.txt

# URL
grep -oE 'https?://[A-Za-z0-9./?=_-]+' file.txt

# Date (DD/MM/YYYY)
grep -oE '[0-9]{2}/[0-9]{2}/[0-9]{4}' file.txt

# MAC Address
grep -oE '([0-9A-Fa-f]{2}:){5}[0-9A-Fa-f]{2}' file.txt
```

---

# GREP — căutare în text

## Opțiuni esențiale

```
┌────────┬────────────────────────────────────────────────────────────────┐
│ Option │ Description                                                    │
├────────┼────────────────────────────────────────────────────────────────┤
│   -i   │ Case-insensitive                                               │
│   -v   │ Invert match (lines that do NOT match)                         │
│   -c   │ Count only (number of matching lines)                          │
│   -n   │ Show line numbers                                              │
│   -l   │ Show only file names with matches                              │
│   -L   │ Show only file names WITHOUT matches                           │
│   -r   │ Recursive (all files in directories)                           │
│   -o   │ Show only the matched portion                                  │
│   -E   │ Extended regex (ERE) — RECOMMENDED                             │
│   -w   │ Match whole words only                                         │
│   -A n │ Show n lines AFTER match                                       │
│   -B n │ Show n lines BEFORE match                                      │
│   -C n │ Show n lines of context (before and after)                     │
│   -h   │ Suppress filename (when searching multiple files)              │
│   -q   │ Quiet mode (for scripts — exit code only)                      │
└────────┴────────────────────────────────────────────────────────────────┘
```

## Exemple practice

```bash
# Căutare fără sensibilitate la majuscule/minuscule
grep -i 'error' log.txt

# Numără liniile care se potrivesc
grep -c 'ERROR' log.txt

# Căutare recursivă în toate fișierele .py
grep -rn 'def ' --include='*.py' .

# Linii care conțin "error", dar NU "debug"
grep 'error' log.txt | grep -v 'debug'

# Context: 2 linii înainte și după
grep -C 2 'CRITICAL' log.txt

# Extrage doar adresele IP
grep -oE '([0-9]{1,3}\.){3}[0-9]{1,3}' access.log
```

---

# SED — editor de flux (stream)

## Sintaxă de bază

```
sed [options] 'command' file
sed [options] 's/pattern/replacement/flags' file
```

## Comenzi uzuale

```
┌─────────┬────────────────────────────────────────────────────────────────┐
│ Command │ Description                                                    │
├─────────┼────────────────────────────────────────────────────────────────┤
│    s    │ Substitute: s/old/new/                                         │
│    d    │ Delete line                                                    │
│    p    │ Print line (used with -n)                                      │
│    i    │ Insert BEFORE line                                             │
│    a    │ Append AFTER line                                              │
│    c    │ Replace entire line                                            │
│    y    │ Transliterate characters (like tr)                             │
│    q    │ Quit (stop processing)                                         │
└─────────┴────────────────────────────────────────────────────────────────┘
```

## Flag‑uri pentru substituție

```
┌──────┬─────────────────────────────────────────────────────────────────┐
│ Flag │ Description                                                     │
├──────┼─────────────────────────────────────────────────────────────────┤
│  g   │ Global (all occurrences on line)                                │
│  i   │ Case-insensitive (GNU sed)                                      │
│  p   │ Print if substitution made                                      │
│  w   │ Write to file                                                   │
│  n   │ Replace only nth occurrence                                     │
└──────┴─────────────────────────────────────────────────────────────────┘
```

## Exemple practice

```bash
# Înlocuire simplă
sed 's/old/new/' file.txt

# Înlocuiește TOATE aparițiile
sed 's/old/new/g' file.txt

# Editare in-place (ATENȚIE!)
sed -i 's/old/new/g' file.txt

# Șterge liniile care conțin "debug"
sed '/debug/d' log.txt

# Șterge liniile goale
sed '/^$/d' file.txt

# Șterge comentariile (liniile care încep cu #)
sed '/^#/d' config.txt

# Afișează doar liniile 10–20
sed -n '10,20p' file.txt

# Înlocuire cu referințe inverse (backreferences)
sed -E 's/([0-9]+)-([0-9]+)/\2-\1/g' file.txt
```

---

# AWK — procesare structurată

## Sintaxă de bază

```
awk [options] 'pattern {action}' file
awk -F'delimiter' 'pattern {action}' file
```

## Variabile speciale

```
┌──────────┬────────────────────────────────────────────────────────────┐
│ Variable │ Description                                                │
├──────────┼────────────────────────────────────────────────────────────┤
│   $0     │ Entire line                                                │
│   $1-$n  │ Fields 1 to n                                              │
│   NF     │ Number of Fields (on current line)                         │
│   NR     │ Record Number (line number, global)                        │
│   FNR    │ Record Number in current file                              │
│   FS     │ Field Separator (default: space)                           │
│   OFS    │ Output Field Separator                                     │
│   RS     │ Record Separator (default: newline)                        │
│   ORS    │ Output Record Separator                                    │
│ FILENAME │ Name of current file                                       │
└──────────┴────────────────────────────────────────────────────────────┘
```

## Structura programului

```awk
BEGIN { 
    # Rulează ÎNAINTE de procesarea liniilor
    FS = ","  # Setează separatorul de câmp
}

/pattern/ { 
    # Rulează pentru liniile care se potrivesc pattern‑ului
    print $1, $2 
}

{ 
    # Rulează pentru FIECARE linie
    sum += $3 
}

END { 
    # Rulează DUPĂ ce toate liniile au fost procesate
    print "Total:", sum 
}
```

## Exemple practice

```bash
# Afișează coloana 2
awk '{print $2}' file.txt

# CSV: afișează coloanele 1 și 3
awk -F, '{print $1, $3}' data.csv

# Suma coloanei 3 (sari peste antet)
awk -F, 'NR > 1 {sum += $3} END {print sum}' data.csv

# Media
awk -F, 'NR > 1 {sum += $3; count++} END {print sum/count}' data.csv

# Filtru: salariu > 5000
awk -F, '$4 > 5000 {print $2, $4}' employees.csv

# Număr pe grup
awk -F, 'NR > 1 {count[$3]++} END {for (k in count) print k, count[k]}' data.csv

# Format personalizat de output
awk -F, 'NR > 1 {printf "%-20s %8.2f\n", $2, $4}' employees.csv
```

---

# NANO — editor simplu

## Scurtături esențiale

```
┌──────────────┬─────────────────────────────────────────────────────────┐
│ Shortcut     │ Action                                                  │
├──────────────┼─────────────────────────────────────────────────────────┤
│ Ctrl+O       │ Save (Write Out)                                        │
│ Ctrl+X       │ Exit                                                    │
│ Ctrl+K       │ Cut line                                                │
│ Ctrl+U       │ Paste                                                   │
│ Ctrl+W       │ Search                                                  │
│ Ctrl+\       │ Search and Replace                                      │
│ Ctrl+G       │ Help                                                    │
│ Ctrl+_       │ Go to line                                              │
│ Alt+U        │ Undo                                                    │
│ Alt+E        │ Redo                                                    │
│ Ctrl+C       │ Show cursor position                                    │
└──────────────┴─────────────────────────────────────────────────────────┘
```

---

# Ghid rapid de decizie

```
┌─────────────────────────────────────────────────────────────────────────┐
│  WHAT DO YOU WANT TO DO?              │  WHAT DO YOU USE?               │
├────────────────────────────────────┼────────────────────────────────────┤
│ Search text                        │ grep                               │
│ Extract patterns                   │ grep -o                            │
│ Replace text                       │ sed s///                           │
│ Delete lines                       │ sed d or grep -v                   │
│ Process columns                    │ awk                                │
│ Calculations and aggregations      │ awk                                │
│ Complex transformations            │ awk                                │
│ Quick file edit                    │ nano                               │
│ One-time search/replace            │ sed                                │
└────────────────────────────────────┴────────────────────────────────────┘
```

---

```
╔══════════════════════════════════════════════════════════════════════════╗
║                                                                          ║
║     "With grep you search, with sed you transform, with awk you analyse."║
║                                                                          ║
║                        — Operating Systems, ASE-CSIE                     ║
║                                                                          ║
╚══════════════════════════════════════════════════════════════════════════╝
```

---

*Fișă de sinteză pentru Seminarul 4 de Sisteme de Operare | ASE București - CSIE*

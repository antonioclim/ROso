# Cheat Sheet Vizual - Seminarul 3-4
## Sisteme de Operare | Operatori, Redirecționare, Filtre, Bucle

**Document**: S02_09_VISUAL_CHEAT_SHEET.md  
**Format**: Optimizat pentru printare pe A4 (orientare orizontală)  
**Versiune**: 1.0

---

## OPERATORI DE CONTROL

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        OPERATORI DE CONTROL                                  │
├─────────┬───────────────────────────────────────────────────────────────────┤
│    ;    │ SECVENȚIAL - execută ambele, indiferent de rezultat               │
│         │   cmd1 ; cmd2 ; cmd3                                              │
│         │   Ex: echo "Start" ; ls ; echo "Done"                             │
├─────────┼───────────────────────────────────────────────────────────────────┤
│   &&    │ AND - execută cmd2 DOAR dacă cmd1 reușește (exit 0)               │
│         │   cmd1 && cmd2                                                    │
│         │   Ex: mkdir dir && cd dir  (intră doar dacă s-a creat)           │
├─────────┼───────────────────────────────────────────────────────────────────┤
│   ||    │ OR - execută cmd2 DOAR dacă cmd1 eșuează (exit ≠ 0)               │
│         │   cmd1 || cmd2                                                    │
│         │   Ex: ping host || echo "Offline"                                 │
├─────────┼───────────────────────────────────────────────────────────────────┤
│   &     │ BACKGROUND - execută cmd în fundal, returnează imediat            │
│         │   cmd &                                                           │
│         │   Ex: sleep 100 &  → [1] 12345                                   │
├─────────┼───────────────────────────────────────────────────────────────────┤
│    |    │ PIPE - stdout cmd1 → stdin cmd2                                   │
│         │   cmd1 | cmd2 | cmd3                                              │
│         │   Ex: cat file | sort | uniq                                      │
└─────────┴───────────────────────────────────────────────────────────────────┘
```

### Pattern-uri Comune

```bash
# If-then-else pe o linie
cmd && echo "OK" || echo "FAIL"

# Error handler cu exit
cmd || { echo "Eroare!"; exit 1; }

# Creare condițională
[ -d "dir" ] || mkdir dir

# Verificare înainte de acțiune
[ -f "file" ] && cat file
```

### Grupare Comenzi

```
┌──────────────────────────────────────────────────────────────────┐
│   { cmd1; cmd2; }    │  În SHELL CURENT - modificările persistă  │
│   ( cmd1; cmd2 )     │  În SUBSHELL - modificările se pierd      │
└──────────────────────────────────────────────────────────────────┘

Exemplu:
x=1                          y=1
{ x=2; echo $x; }           ( y=2; echo $y; )
echo $x  # → 2              echo $y  # → 1 !!!
```

---

## REDIRECȚIONARE I/O

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           FILE DESCRIPTORS                                   │
│                                                                             │
│              ┌──────────────┐                                               │
│   stdin ──►  │    PROCES    │ ──► stdout                                    │
│    (0)       │              │      (1)                                      │
│              │              │ ──► stderr                                    │
│              └──────────────┘      (2)                                      │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────┬───────────────────────────────────────────────────────────────────┐
│ OUTPUT  │                                                                   │
├─────────┼───────────────────────────────────────────────────────────────────┤
│   >     │ Suprascrie fișier (creează dacă nu există)                        │
│         │   echo "text" > file.txt                                          │
├─────────┼───────────────────────────────────────────────────────────────────┤
│   >>    │ Adaugă la fișier (append)                                         │
│         │   echo "linie nouă" >> file.txt                                   │
├─────────┼───────────────────────────────────────────────────────────────────┤
│   2>    │ Redirecționează stderr                                            │
│         │   cmd 2> errors.log                                               │
├─────────┼───────────────────────────────────────────────────────────────────┤
│  2>&1   │ stderr → unde e stdout ACUM                                       │
│         │   cmd > all.log 2>&1   (ambele în all.log)                       │
├─────────┼───────────────────────────────────────────────────────────────────┤
│   &>    │ AMBELE (stdout + stderr) în fișier (scurtătură Bash)              │

*Notă personală: Bash-ul are o sintaxă urâtă, recunosc. Dar rulează peste tot, și asta contează enorm în practică.*

│         │   cmd &> all.log                                                  │
└─────────┴───────────────────────────────────────────────────────────────────┘

┌─────────┬───────────────────────────────────────────────────────────────────┐
│ INPUT   │                                                                   │
├─────────┼───────────────────────────────────────────────────────────────────┤
│   <     │ Citește din fișier                                                │
│         │   sort < unsorted.txt                                             │
├─────────┼───────────────────────────────────────────────────────────────────┤
│   <<    │ Here Document - input inline până la delimiter                    │
│         │   cat << EOF                                                      │
│         │   Linia 1                                                         │
│         │   EOF                                                             │
├─────────┼───────────────────────────────────────────────────────────────────┤
│  <<<    │ Here String - string ca input                                     │
│         │   tr 'a-z' 'A-Z' <<< "hello"                                     │
└─────────┴───────────────────────────────────────────────────────────────────┘
```

### Capcană: Ordinea Contează!

```bash
# CORECT: Ambele în fișier
cmd > file 2>&1

# GREȘIT: stderr rămâne pe terminal!
cmd 2>&1 > file
```

### Suprimare Output

```bash
cmd > /dev/null       # Aruncă stdout
cmd 2> /dev/null      # Aruncă stderr
cmd &> /dev/null      # Aruncă totul
```

---

## FILTRE DE TEXT

```
┌───────────────────────────────────────────────────────────────────────────────┐
│                              FILTRE                                           │
│                                                                               │
│    stdin ──► │ FILTRU │ ──► stdout                                           │
│                                                                               │
│    Filozofia UNIX: fiecare filtru face UN lucru și îl face bine              │
└───────────────────────────────────────────────────────────────────────────────┘

┌─────────┬──────────────────────────────────────────────────────────────────────┐
│  sort   │ Sortează linii                                                       │
│         │   sort file              # alfabetic                                 │
│         │   sort -n file           # numeric                                   │
│         │   sort -r file           # descrescător                              │
│         │   sort -k2 file          # după coloana 2                            │
│         │   sort -t',' -k3 file    # CSV, coloana 3                            │
│         │   sort -u file           # elimină duplicate                         │
├─────────┼──────────────────────────────────────────────────────────────────────┤
│  uniq   │ Elimină duplicatele CONSECUTIVE  ⚠️ Folosește sort înainte!          │
│         │   sort file | uniq       # elimină duplicate                         │
│         │   sort file | uniq -c    # numără apariții                           │
│         │   sort file | uniq -d    # doar duplicatele                          │
├─────────┼──────────────────────────────────────────────────────────────────────┤
│   cut   │ Extrage coloane/caractere                                            │
│         │   cut -d':' -f1 file     # câmpul 1, delimitator :                   │
│         │   cut -d',' -f1,3 file   # câmpurile 1 și 3                          │
│         │   cut -c1-10 file        # caracterele 1-10                          │
│         │   ⚠️ Delimitator implicit = TAB, nu spațiu!                          │
├─────────┼──────────────────────────────────────────────────────────────────────┤
│  paste  │ Îmbină coloane din fișiere                                           │
│         │   paste f1 f2            # îmbină orizontal                          │
│         │   paste -d',' f1 f2      # cu delimitator                            │
│         │   paste -s file          # serializare pe o linie                    │
├─────────┼──────────────────────────────────────────────────────────────────────┤
│   tr    │ Translatează/șterge caractere (NU stringuri!)                        │
│         │   tr 'a-z' 'A-Z'         # lowercase → UPPERCASE                     │
│         │   tr -d '0-9'            # șterge cifre                              │
│         │   tr -s ' '              # comprimă spații multiple                  │
│         │   tr -cd '[:alnum:]'     # păstrează DOAR alfanumerice               │
├─────────┼──────────────────────────────────────────────────────────────────────┤
│   wc    │ Numără                                                               │
│         │   wc -l file             # linii                                     │
│         │   wc -w file             # cuvinte                                   │
│         │   wc -c file             # bytes                                     │
├─────────┼──────────────────────────────────────────────────────────────────────┤
│  head   │ Începutul fișierului                                                 │
│         │   head -n 5 file         # primele 5 linii                           │
│         │   head -n -5 file        # toate EXCEPTÂND ultimele 5                │
├─────────┼──────────────────────────────────────────────────────────────────────┤
│  tail   │ Sfârșitul fișierului                                                  │
│         │   tail -n 5 file         # ultimele 5 linii                          │
│         │   tail -n +5 file        # de la linia 5 până la final               │
│         │   tail -f file           # 👁️ FOLLOW - monitorizare live             │
├─────────┼──────────────────────────────────────────────────────────────────────┤
│   tee   │ Duplică stream: afișează ȘI salvează                                 │
│         │   cmd | tee file         # stdout → file + ecran                     │
│         │   cmd | tee -a file      # append                                    │
└─────────┴──────────────────────────────────────────────────────────────────────┘
```

### Pipeline-uri Comune

```bash
# Top 10 cele mai frecvente linii
sort file | uniq -c | sort -rn | head -10

# Top 5 useri după procese
ps aux | awk '{print $1}' | sort | uniq -c | sort -rn | head -5

# Extrage IP-uri unice din log
cut -d' ' -f1 access.log | sort -u

# Numără fișierele .txt
find . -name "*.txt" | wc -l
```

---

## BUCLE

```
┌───────────────────────────────────────────────────────────────────────────────┐
│                               FOR LOOP                                         │
├───────────────────────────────────────────────────────────────────────────────┤
│  for var in lista; do                                                         │
│      comenzi                                                                   │
│  done                                                                          │
│                                                                               │
│  # Exemple:                                                                   │
│  for i in 1 2 3; do echo $i; done                                            │
│  for i in {1..10}; do echo $i; done          # brace expansion               │
│  for i in {0..100..10}; do echo $i; done     # cu pas                        │
│  for f in *.txt; do cat "$f"; done           # peste fișiere                 │
│                                                                               │
│  # Stil C:                                                                   │
│  for ((i=0; i<10; i++)); do                                                  │
│      echo $i                                                                  │
│  done                                                                         │
├───────────────────────────────────────────────────────────────────────────────┤
│  ⚠️ Capcană: for i in {1..$N} NU funcționează cu variabile!                   │
│              Brace expansion se face ÎNAINTE de variable expansion           │
│              Soluție: for i in $(seq 1 $N) sau for ((i=1; i<=N; i++))       │
└───────────────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────────────┐
│                             WHILE / UNTIL                                      │
├───────────────────────────────────────────────────────────────────────────────┤
│  while [ condiție ]; do         │  until [ condiție ]; do                     │
│      comenzi                     │      comenzi                                │
│  done                            │  done                                       │
│                                  │                                             │
│  # Execută CÂT TIMP e TRUE       │  # Execută CÂT TIMP e FALSE                │
├───────────────────────────────────────────────────────────────────────────────┤
│  # Citire fișier linie cu linie:                                             │
│  while IFS= read -r line; do                                                  │
│      echo "$line"                                                             │
│  done < file.txt                                                              │
│                                                                               │
│  ⚠️ SUBSHELL PROBLEM:                                                        │
│  cat file | while read line; do ... done  ← variabilele NU persistă!        │
│  while read line; do ... done < file      ← CORECT                          │
└───────────────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────────────┐
│                           CONTROL FLOW                                         │
├───────────────────────────────────────────────────────────────────────────────┤
│  break      │ Ieșire din buclă (NU din script - pentru asta: exit)           │
│  break N    │ Ieșire din N bucle imbricate                                    │
│  continue   │ Sări la următoarea iterație                                     │
│  continue N │ Continuă bucla N                                                │
└───────────────────────────────────────────────────────────────────────────────┘
```

### Buclă Infinită

```bash
while true; do
    # cod
    sleep 1
done

# sau
while :; do ...; done
for ((;;)); do ...; done
```

---

## coduri de ieșire

```
┌────────┬──────────────────────────────────────────────────────────────────────┐
│  $?    │ Exit code-ul ultimei comenzi                                         │
├────────┼──────────────────────────────────────────────────────────────────────┤
│   0    │ Succes                                                               │
│   1    │ Eroare generală                                                      │
│   2    │ Utilizare greșită                                                    │
│  126   │ Fișier nu e executabil                                               │
│  127   │ Comandă negăsită                                                     │
│ 128+N  │ Terminat de semnal N                                                 │
└────────┴──────────────────────────────────────────────────────────────────────┘

Comenzi utile:
  true         # returnează 0
  false        # returnează 1
  exit N       # ieșire cu cod N

În pipeline:
  $?              # doar ultimul exit code
  ${PIPESTATUS[@]} # toate exit codes
  set -o pipefail # $? = primul non-zero
```

---

## TROUBLESHOOTING RAPID

| Problemă | Cauză | Soluție |
|----------|-------|---------|
| `uniq` nu elimină toate duplicatele | Nu sunt consecutive | `sort \| uniq` |
| `cut -f2` returnează toată linia | Delimitator e TAB, nu spațiu | `cut -d' ' -f2` |
| `for i in {1..$N}` nu funcționează | Brace expansion la parse time | `for i in $(seq 1 $N)` |
| Variabile din `pipe \| while` se pierd | Subshell | `while ... done < file` |

*(Pipe-urile sunt geniul Unix-ului. Combin comenzi simple pentru a rezolva probleme complexe.)*

| `> file 2>&1` vs `2>&1 > file` | Ordinea contează! | Prima variantă e corectă |
| `cmd &` nu termină când închid terminalul | Nu e detașat | `nohup cmd &` |

---

## REFERINȚE RAPIDE

```bash
# Verificări fișiere
[ -f file ]    # fișier există
[ -d dir ]     # director există
[ -r file ]    # poate citi
[ -w file ]    # poate scrie
[ -x file ]    # poate executa

# Comparații numere
[ $a -eq $b ]  # egal
[ $a -ne $b ]  # diferit
[ $a -lt $b ]  # mai mic
[ $a -gt $b ]  # mai mare

# Comparații stringuri
[ "$a" = "$b" ]   # egal
[ "$a" != "$b" ]  # diferit
[ -z "$a" ]       # string gol
[ -n "$a" ]       # string non-gol
```

---

*Cheat Sheet pentru Seminarul 3-4 SO | ASE București - CSIE*  
*Printare recomandată: A4 orizontal, Font 8-9pt*
